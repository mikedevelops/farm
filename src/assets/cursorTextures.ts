import * as hoverCursorTexture from '../../assets/images/textures/cursors/hover.png';
import TextureMap from '../interfaces/TextureMap';

const cursorTextures: TextureMap[] = [
    {
        name: 'cursor_default',
        url: hoverCursorTexture
    }
];

export default cursorTextures;
